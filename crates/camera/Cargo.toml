[package]
name = "camera"
version = "0.1.0"
edition = "2024"

[dependencies]
nokhwa = { version = "0.10", features = ["input-native"] }
tokio = { version = "1.35", features = ["full"] }
shared = { path = "../shared" }