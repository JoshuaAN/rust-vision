[package]
name = "video_stream"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.40", features = ["full"] }
webrtc = "0.11" 
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bytes = "1.5"
axum = { version = "0.7", features = ["ws"] }